TODO list for Perl module Catalyst::Model::SVN:

Pre 0.07 release:
    - Test a root which isn't at /

Pre 0.08 release:
    - Fix propget inefficiency
    - Test resolve_copy in some more unusual situations
    - Test resolve_copy across multiple moves
    - Add helpers tests

Future:
    - Turn item returned by cat into an object?
    - Add write support
